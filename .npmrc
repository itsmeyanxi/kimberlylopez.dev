# The QA scripts drive the system Edge install via Playwright's `channel`
# option, so Playwright's bundled browsers are never used. Skipping the
# download keeps local installs quick and the deploy build small.
playwright_skip_browser_download=1
